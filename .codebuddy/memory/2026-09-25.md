# 2026-09-25

## ai-agent 项目：gui.py 全面重构（1+2+3+4 全做）

对 `c:\Users\114514\vscode_doc\ai-agent` 的 AI 助手桌面程序做了完整改造：

- **安全(1)**：原 `run.bat` 明文硬编码 API Key 已迁移到 `.env`（`OPENAI_API_KEY`/`OPENAI_BASE_URL`/`OPENAI_MODEL`），`.env` 已加入 `.gitignore`。`run.bat` 改为优先用系统环境变量、缺失时读 `.env`（零依赖 `for /f` 读取）。⚠️ 该 Key 仍属「已泄露」状态，已提醒用户在服务商后台吊销轮换。
- **编码(2)**：新增零依赖 `load_env_file()`（`client.py`），`cli.py` 用 `sys.stdout.reconfigure(encoding="utf-8")` 修 Windows GBK 控制台乱码。
- **优化(3)**：`gui.py` 不再用 `subprocess` 拉起 `cli.py`（每次省去启动 Python 进程开销），改为在 `QThread` 内直接调用 `AIClient.stream_chat()` 流式输出；新增「停止」按钮（`requestInterruption`+关闭 stream）、「清空」按钮；线程 `finished.connect(deleteLater)` 修复生命周期。
- **增强(4)**：多轮对话（`self.history` 累积 role/content 传给 API）；新增 `mdrender.py` 零依赖 Markdown→HTML 渲染器（标题/粗斜体/代码块/列表/引用/链接），`outputEdit` 富文本显示并支持流式节流重绘（80ms）。

### 文件变更
- 新增：`mdrender.py`、`.gitignore`、`.env`
- 改写：`client.py`（`stream_chat`/`history`/`.env`）、`cli.py`（UTF-8/多轮）、`gui.py`（流式+多轮+MD+停止/清空）、`gui/main.ui`（加 stopButton/clearButton 的 HBoxLayout）、`run.bat`（去硬编码）
- 冒烟测试：offscreen 下构造 `MainWindow` 验证控件绑定/渲染/history 逻辑通过；`py_compile` 全过；lint 0 错误。

### 约定/偏好
- 用户对该项目采取"全做"的果断风格，直接落地不反复确认。
- 该项目入口为 `run.bat`，现提供选择菜单：输入 1 启动 `py gui.py`（GUI），输入 2 启动 `py cli.py -i`（CLI 交互模式）。

## run.bat 菜单 & 冒烟脚本（同日后续调整）
- `run.bat` 改为带菜单：选 1=GUI（`py gui.py`），选 2=CLI 交互（`py cli.py -i`），环境变量读取逻辑不变。
- 保留 `_smoke.py` 作为离线冒烟测试脚本（offscreen 下构造 `MainWindow`，验证 mdrender/控件绑定/历史累积），用户明确要求保留。

## run.bat 编码坑（重要）
- 中文 Windows 的 `cmd.exe` 解析 .bat 时按 GBK(cp936)，**UTF-8 无 BOM 且含中文的 bat 会被错读**，中文注释/`if exist` 等被错误分词成命令而报一堆乱码错误（如 `'NAI_MODEL...REM'`、`'exist'`、`'柟寮忥細'`）。
- 解决：`run.bat` 改为**纯 ASCII（英文）**即可零风险。若坚持要中文提示，须把 bat 另存为 GBK 或带 BOM 的 UTF-8。
- 末尾的 `libpng warning: iCCP: known incorrect sRGB profile` 是 PyQt 加载 PNG 资源的无害警告，可忽略。

## run.bat 的 .env 重载 bug（同日修复）
- Bug：原 `if not defined OPENAI_API_KEY (...读.env...)` 在**持久终端**里，第一次 run 用 `set` 把变量写进终端会话，导致后续 run 因 `if not defined` 为 false 而**跳过读 .env**，改 .env 不生效，需重启终端才生效。
- 修复：`run.bat` 改为**每次无条件重新读取 `.env` 并覆盖**（`if exist .env` 直接 for/f set），编辑 .env 立即生效，不再依赖重启终端。

## gui.py 状态栏显示生效配置
- `MainWindow.__init__` 状态栏在就绪时显示实际生效的 `模型` 与 `端点`（`OPENAI_MODEL`/`OPENAI_BASE_URL`，缺失则回退 `AIClient.DEFAULT_MODEL`/`DEFAULT_BASE_URL`），方便一眼确认 `.env` 改对。复用常量避免构造 `AIClient` 触发 key 校验。已 py_compile + offscreen 冒烟验证通过。

## QTextEdit 块排版规则（实测，重要）
用户反馈历史框排版粘连（`hiAI： Hello!...`）。offscreen 实验结论（PyQt5）：
- **连续多次 `insertHtml` 会并进同一个块**（'你：','hi','AI：' 全在一行）——即使 HTML 全部闭合。
- 单次 `insertHtml` 内含多个块级标签（`<p>A</p><p>B</p>`）→ 正常分行。
- `QTextEdit.append()` 每次保证从**新段落**开始 → 逐段追加用它。
- **`QTextEdit` 没有 `appendHtml` 方法**（会 AttributeError）。
- 尾部替换（setPosition+removeSelectedText）后直接 `insertHtml` 会粘到上一块（'C1C2'）；**必须先 `cursor.insertBlock()` 再 `insertHtml`**。
- 落地：`_append_user`/`_begin_ai` 用 `append()`；`_render_ai` 替换后 `insertBlock()+insertHtml()`；标签行与 markdown 内容分开插入，避免外层 `<p>` 嵌套内层 `<p>` 的非法 HTML。`_smoke.py` 已加 `LAYOUT_OK` 行序断言。

## 对话持久化（同日新增）
- 需求：对话能保存、关闭后再开继续。
- 实现：`gui.py` 新增 `conversation.json`（项目根，已加入 .gitignore）自动保存/恢复：
  - `HISTORY_FILE` 常量；`_save_history()`（json dump, ensure_ascii=False）、`_load_history()`、`_redraw_all()`（按 history 重绘）。
  - `__init__` 末尾自动 `_load_history()` + `_redraw_all()`，状态栏显示「已恢复 N 轮对话」。
  - `_on_success` 把「user+assistant」成对写入 history 后 `_save_history()`；`_on_error` 不写入（失败请求不污染存档），用 `self._pending_user` 暂存进行中的提问，成功才落盘。
  - `on_clear` 清空内存 + `_save_history()` 写空（下次不恢复）。
  - 新增「导出」按钮（`exportButton`，main.ui 已加），`QFileDialog` 导出 Markdown/纯文本；`on_export` 生成 `# 对话记录` 带 `## 你`/`## AI` 分段。
- `_smoke.py` 追加 `PERSIST_OK` 断言（保存后新建窗口自动恢复）。
- 注：本会话末两次 `py _smoke.py` 因权限超时未执行，待用户手动验证。

## gui.py 多会话管理 + 导入 + 带时间戳导出 + 自定义保存位置（同日升级）
用户要求把「单文件对话持久化」升级为：多会话管理、可导入、导出带时间戳、可自定义自动保存位置。已全部落地并 offscreen 隔离测试通过（`_smoke2.py`，已删除）、py_compile 通过、lint 0 错误。

- **数据结构升级**：单 `conversation.json` → 每会话一个 `session_<id>.json`（`id`=时间戳+uuid6位；含 title/created_at/updated_at/messages）。旧 `conversation.json` 首次启动自动迁移为首会话并删除旧文件（`_migrate_legacy_if_needed`）。
- **左侧会话面板**（main.ui 新增 `leftPanel`：QLabel+QListWidget+「新建/删除」行+「导入/设置」行；`mainHLayout` 水平布局，右侧 `rightPanel` 放原输入/对话区，窗口加宽到 920x600）。
- **多会话逻辑**：`self.sessions`(dict)、`self.session_order`(按 updated_at 倒序)、`self.current_id`；`_create_session`/`_switch_to`/`_on_session_row_changed`（带 `_refreshing` 标志防刷新误触发）/`on_delete_session`（确认弹窗，至少保留1个）。切换前 `_save_current_session`。
- **自定义保存位置**：`settings.json`（项目根，已加 .gitignore）存 `autosave_dir`；「设置」按钮 `QFileDialog.getExistingDirectory` → 写 settings + 把内存会话全部写入新目录 + 重新 `_load_sessions`。默认 `conversations/`（`DEFAULT_AUTOSAVE_DIR`）。
- **导入**：`on_import` 支持 `.md`（按 `## 你`/`## AI` 分段，`parse_markdown_history` 用 `map_role` 映射 user/assistant）和 `.json`（list 或含 messages/history 字段，`parse_json_history`）；模块级 `parse_import_file` 分发。导入即新建会话。
- **导出**：`on_export` 默认文件名 `对话记录_<YYYYmmdd_HHMMSS>`（带时间戳），可选 `.md`（同 `## 你/AI` 格式）或 `.json`。
- 解析/渲染往返、持久化往返均经隔离测试验证（导出 md 文本→`parse_markdown_history` 还原一致）。
- `.gitignore` 追加：`conversations/`、`settings.json`。

## gui.py 进一步升级：折叠 / 网络图片 / 拖拽导入 / 会话重命名（同日）
用户要求把此前提议的两项扩展（拖拽导入 + 会话重命名）一并落地，并新增：左侧会话栏可折叠、支持输入网络图片地址（按 OpenAI 多模态规范）。

- **折叠会话栏**：main.ui 在 `mainHLayout` 首位加 `expandButton`（默认 `visible=false`，文本 `≫`），`leftPanel` 顶部加 `collapseButton`（`⟨ 收起会话`）。`on_collapse`：`leftPanel.hide()`+`expandButton.setVisible(True)`；`on_expand` 反向。
- **网络图片（OpenAI 规范）**：右侧 inputEdit 下加 `imageLabel`+`imageEdit`（提示 http(s) 多图空格分隔）。`on_send` 用 `_parse_image_urls`（正则 `^https?://`，过滤非 http/https）解析，调 `_build_user_content` 构造成 OpenAI 多模态 content 数组：`[{"type":"text","text":prompt},{"type":"image_url","image_url":{"url":u,"detail":"auto"}}]`；无图回退纯字符串（兼容旧逻辑），纯图片（无文字）也必须是数组。`client.py` 无需改动（`_build_messages` 直接透传 content）。
- **渲染**：`_append_user` 现接受 str 或 content 数组，提取文本段渲染 Markdown，并把每张图片以 `[图片] <a href>url</a>` 链接列出（GUI 内不内嵌显示图，仅给可点击地址）。
- **会话重命名**：`sessionList` 双击 → `on_rename_session` 用 `QInputDialog.getText` 改名，写文件并刷新列表（`sessionLabel` 已提示"双击重命名"）。
- **拖拽导入**：`setAcceptDrops(True)`；`dragEnterEvent` 接受含 urls 拖拽；`dropEvent` 对 `.md/.json/.txt` 调 `_do_import`（原为 `on_import` 内联，已抽取复用）。
- 隔离测试（`_smoke2.py`，已删）覆盖：新控件、图片 URL 解析、content 数组构建（纯图/纯文本）、数组渲染、折叠（`isHidden` 判定，因 offscreen 未 show 时 `isVisible` 失真）、重命名落盘；py_compile + lint 0 错误。
